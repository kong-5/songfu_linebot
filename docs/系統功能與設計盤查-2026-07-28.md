# 系統功能與設計地毯式盤查 2026-07-28（六路並行掃描）

本輪目標：**功能完整性 ＋ 專業設計項目**的地毯式搜索。與前三輪報告分工：
- `審查報告-UIUX與邏輯深查-2026-07-10.md`：UI/UX × 邏輯 × 市場對標
- `系統邏輯總檢視-2026-07-14.md`：邏輯 bug 總檢視
- `體質健檢-2026-07-27.md`：結構性體質（漏 await／雙寫／冪等／稽核／XSS）＋拆檔

本輪六路並行：**①LINE 收單域 ②後台訂單/金流/客戶域 ③庫存/盤點/營運支援域
④設計規範遵循＋LIFF UX ⑤資料庫/安全/基礎設施 ⑥凌越串接鏈路＋文件一致性**。
已排除前三輪已列管的 backlog 項目；狀態驗證發現 07-14 報告 §2.6 已修（留窗口）、§2.7 修一半、§2.8 已完成。

> 標記說明：【高】會漏單/錯帳/資料遺失/安全事故；【中】日常作業卡住或累積性風險；【低】品質債。
> 每項附 file:line 證據。多路獨立命中的項目（置信度最高）在 §七 彙總。
> Top 12 的三個最高風險資安項（#1 webhook fail-open、#5 備份無權限、#6 LIFF 盤點無授權）已逐一核對現行程式碼屬實
> （`webhook/line.js:1886-1891` 確為 `next()` 放行、`export-backup.js:116` 確為 `async (_req,res)` 無中介層、
> `liff/index.js:86-92` 確只做 `verifyLineIdToken`）。其餘項目為代理逐檔探查結果，動工時仍請就地覆核。

---

## 執行摘要 — 跨域 Top 12（依風險×成本排序）

| # | 項目 | 出處 | 為什麼是第一批 |
|---|---|---|---|
| 1 | **LINE webhook 未設 secret 時 fail-OPEN 完全不驗簽**（任何人可 POST 偽造建單） | §五 A1 | 一行改動；worker/jobs 都已 fail-closed，唯獨最外層漏掉 |
| 2 | **`ops_alert_group_id` 沒有任何設定入口** → 現有 7 個告警（回寫三振/單號衝突/庫存推送失敗/LINE 漏單）很可能從上線至今全是 no-op | §五 C1、§六 1.1 | 不做這件，其他所有告警都是裝飾 |
| 3 | **手動結單不發訂單明細摘要＋先刪 session 再 rebuild**（rebuild 當機即永久漏單無人知） | §一 A1/A2 | 客戶主動關單反而收不到憑據 |
| 4 | **非 Cloud Tasks 路徑無重試閉環**（enqueue 失敗 fallback 與非 CT 模式都是 `.catch(console.error)` 斷單無告警） | §一 A5/A6 | 漏單直接成因 |
| 5 | **整庫備份 JSON（含全部密碼雜湊）任何登入者可下載**；備份表清單漏掉大半系統、且無還原流程 | §五 A2/B8、§三 B1/B2 | 安全＋災難復原雙重缺口 |
| 6 | **LIFF 盤點/掃碼 API 只驗「是 LINE 用戶」無授權**（外部人可寫盤點、讀全庫存、改條碼） | §五 A7 | 空籃 API 已有正確授權可照抄 |
| 7 | **客訴/作廢單不清凌越佇列**（to-complaint 不清 `lingyue_queued_at`，客訴單照樣被寫進 ERP） | §二 B5 | 直接錯帳 |
| 8 | **re-recognize 對已轉入凌越的單無守衛＋order-detail 整域無稽核**（ctx 沒帶 logDataChange） | §二 B1/B2 | 後台與 ERP 靜默分歧 |
| 9 | **多公司庫存推送部分失敗被後續成功洗掉**（松富失敗報成功）＋回寫「結果不明」回查失敗仍走重複開單老路 | §六 1.4/1.2 | 07-14 §2.6 的殘留窗口 |
| 10 | **部署無金絲雀**（健檢失敗但流量已 100% 導入）＋ `max-instances=1` 不變式只存在 Console | §五 F1/F2 | 壞版本上得去、單實例假設可被靜默打破 |
| 11 | **權限粒度缺口**：批次作廢/硬刪主檔/轉入凌越/庫存調整/匯入/改 prompt 全無角色閘門；權限與認證零測試 | §五 A3、D2 | 金流已有 canCash 範式可延伸 |
| 12 | **設計系統「複製而非引用」**：sf3 有三套逐像素 clone、兩個未定義 class 造成真實破版、全站零 focus 樣式 | §四 | 批次 A+B 半天可拿回大部分 |

---

## 一、LINE 收單域（webhook/line.js ＋ lib/line-*）

### A. 功能缺口

| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| A1 | 高 | 手動結單（「完成」/「以上X收單」）完全不發訂單明細摘要 | `dist/webhook/line.js:1428-1490` | 手動結單先清 autoFinalize timer，30 秒摘要永遠不發；1476 的提醒還叫客戶「對照 30 秒後的明細」。建議把摘要組裝（line.js:265-350）抽成 `sendOrderSummary(orderIds)` 兩路共用 |
| A2 | 高 | 手動結單「先刪 session 再 rebuild」，rebuild 當機即永久漏結單 | `line.js:1431-1432` vs `1440-1455` | 自動結單已改「rebuild 完成才刪」（215-219），手動路徑仍舊順序。照抄自動路徑順序 |
| A3 | 高 | 逾期 session 被靜默丟棄，不補結單也不告警 | `line.js:84-89` | 重啟恢復時 >10 分鐘一律 delete＋continue：空籃沒補、摘要沒發、無告警。改「補跑一次 finalize（rebuild 冪等）＋ops 告警」 |
| A4 | 中 | `line_collect_sessions` 無週期 sweeper、後台無檢視 | `line.js:69-98`（只在啟動呼叫一次） | 加 60 秒 sweeper 掃逾時補 finalize＋後台唯讀清單頁 |
| A5 | 高 | 只有 Cloud Tasks worker 有重試閉環，另兩條路徑「回 200 就消失」 | `line.js:1934`、`1940`（閉環只在 `dist/index.js:133-154`） | 兩處 `.catch(console.error)`，`{failed}` 沒被讀。檢查 failed>0 → notifyOps＋重排，或一律走 Cloud Tasks |
| A6 | 高 | 30 秒自動結單整段失敗只 console.error | `line.js:353-355` | 此時 session 已移除：無重試、無告警、客戶無摘要。catch 加 notifyOps＋保留 session 供 sweeper 重跑 |
| A7 | 高 | 非收單時段訊息被完全丟棄（不留紀錄、不回覆） | `line.js:900-904` | 客戶以為已下單，內勤查不到。休眠時仍寫對話紀錄＋回覆時段說明 |
| A8 | 中 | 未綁定客戶群組只寫待綁定清單無告警，且提示走非 force reply | `line.js:926-936`、`lib/line-order-write.js:364-388` | 首次登錄 notifyOps；「尚未綁定」改 `{force:true}` |
| A9 | 高 | 偵測「取消訂單」只寫 remark：不回覆客戶、不改狀態、無告警 | `line.js:1719-1750` | 若無人看後台就照樣出貨。加客戶回覆＋notifyOps＋後台待確認置頂 |
| A10 | 中 | 客訴/退貨標記後客戶零回饋；管理員推播靠 env 且失敗靜默 | `line.js:1690-1717`、`1499-1556` | 改走 notifyOps（已有 10 分鐘去重），並回客戶確認句 |
| A11 | 中 | 中文數字叫貨（「青江菜三把」）逐則不解析 | `line.js:1754-1758` | `looksLikeOrder` 只認阿拉伯/全角數字。納入一二三…十/兩 |
| A12 | 中 | 解析 0 筆時無任何「怎麼修正」回饋（守則#4） | `line.js:1759-1760`、`1800-1821`、摘要 `308` | 0 筆時回「請用品名＋數量＋單位分行重傳（例：高麗菜 5 公斤）」 |
| A13 | 中 | 非文字非圖片訊息（PDF/檔案/語音/貼圖）靜默丟棄 | `line.js:1128-1131` | 對 file/audio/video 回「目前只讀文字與照片」 |
| A14 | 中 | 圖片下載失敗仍往下走，客戶不知情、無告警 | `line.js:950-967`、`1035-1039` | 失敗即回覆「請重傳」＋notifyOps |
| A15 | 中 | 「今天叫了什麼」只顯示第一張單，與線上改單口徑不一致；數量未走 formatOrderQty | `line.js:1289-1305` vs `1312` | 改用 editOrderRows 同查詢＋連續編號 |
| A16 | 中 | 「改第N項 0」不等於刪除，留下 0 數量品項照出貨單 | `line.js:1362`、`1368` | qty=0 走刪項邏輯（含 delete 軌跡） |
| A17 | 低 | 圖片訊息不寫 `line_conversation_log`，對話時間軸斷裂 | `line.js:938-1127` vs `1666-1677` | 補 `msgType:"image"` 紀錄 |
| A18 | 中 | 圖片路徑無客訴/非訂單意圖判斷（拍壞菜投訴被當叫貨單解析） | `line.js:938-1127`、`lib/parse-order-from-image.js:261-268` | 圖片路徑也跑意圖判斷＋非訂單訊號 |
| A19 | 中 | 整則只有一家子客戶時拆單不通知客戶（通知條件仍 `size>1`） | `line.js:1022`、`1080`、`1243`、`1797` | 任一非空 subKey 即回覆已歸到哪家 |
| A20 | 中 | 非質量單位（箱/包/袋）無 spec 時原樣入庫、不標 need_review | `lib/unit-conversion.js:210-239`、`lib/line-order-write.js:163` | 已對應但單位非 KG 且無換算 → 強制 need_review＋備註 |
| A21 | 高 | `sourceHasExplicitUnit` 整段判斷：混合寫法整批套錯單位（「蔥5 蛋2」全變公斤） | `lib/parse-order-message.js:131`、`135` | 改逐列判斷，無單位列套 fallback＋備註 |
| A22 | 中 | AI 對品未驗證回傳 id 在候選池內；候選硬上限 600 | `lib/resolve-product.js:217`、`229`、`265-268` | 回傳 id 白名單比對；候選改 DB 端 LIKE 先篩 |
| A23 | 中 | 空籃：料號未建檔或客戶無路線時靜默不補 | `lib/empty-baskets.js:18-20`、`32-33` | !prod 改 console.error＋notifyOps；無路線在 remark 標註 |
| A24 | 中 | 「以上X收單」數量校驗只在手動路徑、門檻硬寫 2 | `line.js:1470-1484` | 門檻進 app_settings；自動結單摘要帶「共 N 項」 |
| A25 | 中 | 圖片解析快取鍵不含 prompt/筆跡對照/子客戶設定，改設定後仍命中舊快取 | `lib/parse-order-from-image.js:20-27` | key 併入 prompt version＋knownSubCustomers＋hints hash |
| A26 | 低 | 無整體指令說明入口（指令散在各處） | `line.js:1420-1423` | 加「說明」關鍵字，依 group_features 動態列出可用指令 |

### B. 專業設計項目

| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| B1 | 高 | 結單 rebuild 後品項冪等鍵 `src_line_message_id` 全部遺失（redelivery 再投遞＝整批雙倍） | `lib/rebuild-order-from-sources.js:210` vs `lib/line-order-write.js:133/211` | rebuild rows 帶回來源 message id，至少留批次標記供去重 |
| B2 | 高 | rebuild 的 DELETE-ALL＋INSERT 無稽核（守則#3） | `lib/rebuild-order-from-sources.js:208-216` | 交易內寫 `data_change_log`（action:"rebuild_items"，前後快照） |
| B3 | 高 | 拆單迴圈 `mergeSessionOrderIds` 後沒有 persistCollectSession，重啟後子單變孤兒 | `line.js:1017`、`1021`、`1793`、`1796`、`1818` | merge 後一律 persist（或收進 helper） |
| B4 | 高 | 附件只存 line_message_id、圖片本體從不落地（LINE 保存期限過後永久無法重辨識/舉證） | `db/index.js:228/1152`、下載點 `rebuild:280`、`line.js:952` | 下載成功即壓縮寫 GCS/DB，`order_attachments` 加 storage_uri |
| B5 | 中 | 純文字摘要無長度上限（可超 LINE 5000 字→400），重試又送同一份 | `line.js:312-319`、`336-345` | 超長自動分段；最終失敗 notifyOps |
| B6 | 中 | 收單確認卡缺「確認無誤」正向按鈕，無客戶確認時間戳 | `lib/line-flex-messages.js:97-125` | 加 postback 寫入軌跡，後台顯示綠標 |
| B7 | 中 | Flex 卡每泡截斷 20 項，團膳大單天天核對不完整 | `lib/line-flex-messages.js:8`、`66-74` | 超上限改多泡 carousel 或補完整純文字 |
| B8 | 中 | 空籃寫入無交易、無稽核 | `lib/empty-baskets.js:31-40` | 包 db.transaction＋補稽核 |
| B9 | 中 | 意圖標記 remark 讀改寫非原子；狀態與稽核不同交易、稽核失敗被吞 | `line.js:1729-1733`、`1694/1698`、`1517/1519` | UPDATE＋稽核包同交易；remark 改原子 UPDATE |
| B10 | 中 | 早期客訴偵測會把「不限日期的最後一張單」改 complaint（可能是三週前已出貨單） | `line.js:1509-1514` | 限近 N 天；超出只稽核＋告警；後台加還原鈕 |
| B11 | 中 | 所有外部呼叫（Gemini/LINE 圖片下載）無 timeout，hang 住超過租約 10 分鐘＝同訊息雙跑 | `lib/gemini-order-helpers.js:412`、`lib/gemini-chat.js:18`、`line.js:952`、`rebuild:280` | 統一 fetchWithTimeout（30-60s）＋Gemini Promise.race |
| B12 | 低 | 429 固定等 5 秒換模型，無指數退避/抖動，hitQuota 不告警 | `lib/gemini-order-helpers.js:447-452` | 指數退避＋notifyOps |
| B13 | 中 | 多數 `reply()` 沒帶 pushTo，replyToken 逾時（AI 解析 10-40 秒後）就靜默消失 | `line.js:1959-1990`；重要回覆 `1136/1350/1381/1405` | 「客戶必須知道」的回覆一律帶 `{pushTo:groupId}` |
| B14 | 中 | 「訂單已成立」10 分鐘確認計時器不持久化，重啟永不補發 | `line.js:100`、`364-401` | 併入 line_collect_sessions，restore 時重排 |
| B15 | 中 | `getTaipeiOrderDate()` 依賴容器 TZ=UTC，換 TZ 整天算錯 | `line.js:134-142`、`700-701`、`781-782`、`1466` | 改 Intl.DateTimeFormat("en-CA") 取日（line-bot-control.js:93-98 已是正解），抽共用 helper |
| B16 | 中 | 記憶體計時器 vs CPU throttling；`--max-instances=1`/`--no-cpu-throttling` 不在部署參數 | `cloudbuild.yaml:71-84` | deploy 明確帶參數（併 §五 F2 處理） |
| B17 | 低 | session 持久化「先 DELETE 後 INSERT」不在交易 | `line.js:50-56` | 包交易或 ON CONFLICT 雙分支（group-features.js:67-77 可抄） |
| B18 | 低 | `processed_line_messages` 無 TTL 永久成長 | `line.js:564-605`、`db/index.js:303/1127` | 每日清理留 30 天（併 §五 B7 retention job） |
| B19 | 低 | `typeof db.transaction==="function"` 不成立時靜默降級非原子（五處） | `line.js:497/1347/1378`、`line-order-write.js:225`、`rebuild:218` | 改 throw 或啟動檢查，不留靜默降級 |
| B20 | 低 | 訂單內容/userId 明文全量進 Cloud Logging | `gemini-order-helpers.js:429`、`line.js:626/1133/915-919` | raw JSON 限 LINE_DEBUG=1；id 只印前後 6 碼 |
| B21 | 高→§五A1 | 未設 channel secret 時 webhook 不驗簽仍受理 | `line.js:1886-1891` | 比照 worker 回 503（見執行摘要 #1） |
| B22 | 低 | 收單域零可觀測指標（成功率/失敗清單只能翻 log） | `line.js:402-406` | 輕量 `line_intake_metrics`＋後台日/週統計頁 |

---

## 二、後台訂單／金流／客戶域

### A. 功能缺口

| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| A1 | 高 | 訂單「改客戶」完全沒有入口（綁錯群組/AI 掛錯客戶只能作廢重打） | orders.js、order-detail.js 全檔零 `UPDATE orders SET customer_id` | 加 `POST /orders/:orderId/set-customer`（稽核＋交易＋重算空籃） |
| A2 | 高 | 訂單無全域搜尋，只有日期區間＋前端客戶名 filter（LIMIT 300） | `orders.js:30-54`、`472-486` | 加後端 `q`（order_no/凌越單號/客戶/品名），不受日期限制 |
| A3 | 高 | 待對應清單無篩選無分頁，且未排除作廢/客訴單（與 orders.js:358 計數口徑打架） | `review.js:20-27` | 補 status 排除＋日期/客戶篩選＋LIMIT 200 |
| A4 | 高 | 收款不支援部分收款/沖抵（已收過直接 409；alloc 一律全額） | `cash.js:826-830`、`846-847` | alloc_amount 開放輸入（≤未收餘額），以累計判定未收 |
| A5 | 高 | 無應收帳齡表/客戶對帳單（跨日未收平鋪一坨） | `cash.js:456-460`、`1069-1108` | 加 `/cash/ar`（客戶彙總＋30/60/90 帳齡＋單客對帳單列印） |
| A6 | 高 | 「重新取單」會刪掉已收款的銷貨單→孤兒收款無偵測 | `lingyue-writeback.js:506`＋`cash.js:826-830` | ingest 交易內比對將刪 sp_no 是否有 alloc，有則保留＋紅色待處理清單 |
| A7 | 中高 | 按錯「轉入凌越」無法取消排隊（代理離線期掛佇列，上線照寫） | 排隊 `order-detail.js:96`；清除只在 `lingyue-writeback.js:185` | 加 `lingyue-cancel-queue`（未寫入才可用＋稽核） |
| A8 | 中 | 缺「未轉入凌越」篩選與批次轉入（30 張單按 30 次） | `orders.js:493-501`、`317-319` | 狀態下拉加選項＋批次排入（沿用缺料號守衛） |
| A9 | 中 | 票據登記後失聯：無到期/兌現/退票追蹤 | `cash.js:849-851`、`1081` | cash_check 加 status＋`/cash/checks` 到期清單 |
| A10 | 中 | 客訴閉環欠分類/責任/賠償金額/SLA；處理備註單欄覆寫 | `complaints.js:73-108`、`584-599` | 加 category/liability/compensation；note 改 append-only |
| A11 | 中 | 客訴對話時間軸可能整段空白（先 LIMIT 200 全群組再 JS filter） | `complaints.js:434-444` | groupId 條件下推 SQL |
| A12 | 中 | 誤建空白客訴無法刪除；「還原為訂單」生出無品項訂單進審核清單 | `complaints.js:49-71`、`313-351` | 加作廢客訴；to-order 對無品項 manual_new 先擋 |
| A13 | 中 | 客戶列表只能名稱查、全表輸出無分頁（每列內嵌 12 個 data-*） | `customers.js:651-653`、`703-711` | 搜尋擴欄位＋LIMIT 200 |
| A14 | 中 | 品項主檔全表載入後 JS 篩選；搜尋不吃別名 | `products.js:232-240` | 後端 LIKE＋LIMIT＋union 別名命中 |
| A15 | 中 | 有全域俗名的品項永遠刪不掉，錯誤訊息指向錯誤方向（守則#4） | `products.js:1304-1311`、`1329`（漏清 `product_aliases`） | 交易內一併刪俗名；失敗訊息回具體引用表名 |
| A16 | 中 | quick-add 只查品名不查凌越料號重複（同料號兩筆→比對/庫存隨機挑） | `products.js:483-489` | erp_code 非空即查重＋可行動訊息 |
| A17 | 中 | 批次操作上限 200 與部分失敗都靜默（一律回 ok） | `orders.js:989/1073/1111`、每筆 catch 只 console | redirect 帶 ok/fail 筆數；超 200 明確擋 |
| A18 | 中 | 揀貨單/凌越 xlsx 超 50 筆靜默截斷；xlsx 對無明細單照蓋「已匯出」章、缺料號照匯 | `orders.js:1141/1327`、`1397-1409`（對照揀貨單已修的 `1172`） | 改 renderedIds 蓋章＋缺料號先擋＋超量提示 |
| A19 | 中 | 訂單層備註（寫進凌越 DocRemark）後台不可編輯；合併 xlsx 直接 docRemark="" | `index.js:4264`、`orders.js:1367` | 明細頁加備註欄＋稽核；xlsx 帶 order.remark |
| A20 | 低 | 額外收入只能刪除重建不能修改 | `cash.js:1138-1145`、`1217/1261` | 加 update 端點（舊值新值稽核） |
| A21 | 低 | 表單模板品項清單硬編碼（新模板要改碼部署） | `lib/order-form-templates.js:43-107` | 移 DB/app_settings＋後台維護頁 |
| A22 | 低 | 訂單統計卡連結全是 `#` | `orders.js:445-449` | 連到 `?status=` 篩選或改非連結 |

### B. 專業設計項目

| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| B1 | 高 | order-detail 整域無稽核能力（ctx 沒帶 logDataChange）：re-recognize/rotated/lingyue-transfer 全無軌跡 | `order-detail.js:21`、`2067-2101`、`41-114` | ORDERS_CTX 傳入 logDataChange，三端點補前後快照 |
| B2 | 高 | re-recognize 對已確認/已轉入凌越的單無守衛（照樣 DELETE+INSERT，ERP 從此不一致） | `rebuild:337-341`、`201-207`、`order-detail.js:2082` | 覆檢加 `lingyue_doc_no IS NULL`；已轉入要求 force＋明確警告 |
| B3 | 高 | `/alias` 無條件回頭改寫全庫歷史 order_items（含已轉凌越/作廢單），無交易無稽核無筆數回報 | `review.js:158-164` | 限未確認未轉入單；與 alias INSERT 同交易；回報 changes |
| B4 | 高 | 客戶主檔新增/編輯/啟停用完全無稽核（決定收單歸屬與路線） | `customers.js:74`、`549`、`1258/1270` | 各補 logDataChange（before/after diff 照 products.js:1213 寫法） |
| B5 | 高 | 凌越回寫佇列只排除 deleted：客訴單照樣被寫進 ERP；void/unvoid 不清/不管佇列欄位 | `lingyue-writeback.js:110`、`:30`；`complaints.js:29-31`；`orders.js:1027/1050` | WHERE 改 NOT IN ('deleted','complaint')；轉客訴/作廢同句清 queued/claimed |
| B6 | 中 | /wait 交給代理前不再檢查缺料號（排隊後品項變動可送空料號進凌越） | `lingyue-writeback.js:123-129` vs `order-detail.js:83-89` | 認領後同檢 missing_count，>0 釋放＋告警 |
| B7 | 中高 | getNextOrderNoAdmin 在交易內吞例外＝PG 交易毒化（25P02） | `index.js:163-165`；呼叫鏈 `order-detail.js:2239/2297`→`index.js:186-200` | fallback 抽交易外，或交易 handle 時直接上拋 |
| B8 | 中 | 破壞性操作無權限粒度（任何帳號可批次作廢 200 張/硬刪主檔/寫凌越） | `cash.js:16-28` 有 requireCash；orders/customers/order-detail 全無 | 至少對作廢/硬刪/轉凌越套 requireManager 或新旗標（併 §五 A3） |
| B9 | 中 | 明細儲存無樂觀鎖，兩人同時編輯互相覆蓋 | `orders.js:1526`、`1556-1601` | 表單帶 updated_at 版本戳，不符回「已被他人修改」 |
| B10 | 中 | 批次操作無交易原子性（中斷留半套、畫面報成功）；batch-change-date 不更新 updated_at | `orders.js:989-1006`、`1073-1095`、`1111-1128` | 整批包交易＋失敗回報 |
| B11 | 中 | 收款金額無數值驗證（負數/超大值照收）；溢短收無門檻覆核 | `cash.js:445`、`803-805`、`837` | ≥0＋上限驗證；\|diff\| 超門檻必填原因 |
| B12 | 中 | 收款頁一次撈整公司收款分配全表（無日期條件，只增不減） | `cash.js:452` | 限當日＋priorUnpaid 的 sp_no 集合 |
| B13 | 中 | 斷號原因/收款客戶主檔/司機名單/路線帶入全無稽核 | `cash.js:339-340`、`1031-1032`、`1043`、`1055-1058` | 四處補 logDataChange |
| B14 | 中 | 訂單稽核卡漏 `order_item_product`/`complaint` 類型；meta_json LIKE 全表掃 | `order-detail.js:1890-1894`；寫入在 `orders.js:902` | 條件補齊；長期 order_id 提實欄位＋索引 |
| B15 | 中 | customer-profile 九處靜默吞例外，輸出餵 Gemini（壞掉＝畫像缺段→辨識率下降無訊號） | `lib/customer-profile.js:44,59,72,84,99,134,152` | 全改 console.warn 帶欄位名＋頁面顯示讀取失敗 |
| B16 | 中 | 客戶 360 評分用 LIMIT 30 截斷清單長度當「累計客訴數」（評分虛高、兩處算法分岔） | `customers.js:192-197`、`225` vs `lib/customer-scoring.js:120-121` | 改呼叫 fetchCustomerScoringInputs 或 COUNT(*) |
| B17 | 中 | 客戶專用別名增刪無稽核；任何錯誤都回「已存在相同別名」（守則#4） | `customers.js:600-607`、`618-619` | 先查重區分 dup；補稽核 |
| B18 | 中 | 品項 quick-add 與 toggle active 無稽核（直接改變收單可對應範圍） | `products.js:489`、`1252/1264` | 補 logDataChange |
| B19 | 中 | conversion_kg 無數值驗證（NaN/負/超大照落地，整單數量可爆） | `products.js:590`、`596-604` | Number.isFinite＋0<x≤1000＋可行動訊息 |
| B20 | 低 | 客訴列表日期字串直接串 SQL（有正則守但風格分岔） | `complaints.js:142`（正解在同檔 110-119） | 改參數化 |
| B21 | 低 | 凌越比對對齊：稽核在交易外逐筆補寫＋TOCTOU | `products.js:95-111`、`123-135` | 稽核移進 chunk 交易；舊值條件樂觀鎖 |
| B22 | 低 | newId 同毫秒亂序 vs display_order NULL 退回 id 序（揀貨單品項順序可與原文不同） | `lib/id.js:4-5`；`orders.js:1192`、`order-detail.js:143`、`lingyue-writeback.js:44` | 新品項一律寫 display_order；隨機段改 crypto |

---

## 三、庫存／盤點／營運支援域

### A. 功能缺口

| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| A1 | 高 | 盤點無鎖定/複核簽核狀態機（status 永遠 'submitted'；三年前的盤點今天照樣能改） | `lib/stocktake-api.js:200-201`、`inventory.js:778-810` | 加 locked＋鎖定人/時間；locked 後 count-edit 拒絕需先解鎖（稽核） |
| A2 | 高 | 效期批號只收集完全不追蹤到期（全 repo 無任何查詢讀 expiry_json 判斷到期） | `stocktake-api.js:100-108`、`inventory.js:285`、`817-835` | 加近效期清單頁＋每日 LINE 推播 |
| A3 | 高 | 盤點重送整場硬刪舊場次零軌跡（LIFF 重送一次洗掉前一版） | `stocktake-api.js:198-199` | 舊 session 標 superseded 保留或整批寫 audit |
| A4 | 中 | `stocktake_count_audit` 只寫不讀，無檢視介面 | `inventory.js:805`（唯一寫入處） | 每日盤點頁加修改軌跡抽屜或併 /audit |
| A5 | 高 | 盤差無處理工作流（推 LINE 完就斷：無處理人/查證結果/結案） | `inventory.js:892-1039` | 加 `stocktake_anomaly_case`（open/checked/closed），排查表只列 open |
| A6 | 高 | 庫存調整無生命週期管理（「凌越重整後記得刪」只是文案；忘刪＝雙重補償＋盤差假性歸零） | `inventory.js:766`、`721-775` | 清單加「已掛 N 天」＋review_due＋超期黃標告警 |
| A7 | 中 | 儀表板「每日盤點」卡是四條死連結假清單，無任何盤點 KPI（stock-mustcount require 了沒用） | `dashboard.js:416`、`13` | 改真 KPI：已盤/應盤倉數、盤差項數、必盤未盤數 |
| A8 | 中 | 儀表板缺庫存快照時效/代理健康度（紅字只在目前庫存頁） | `inventory.js:2762-2789`、`dashboard.js:308-320` | readStockMeta 心跳搬 KPI 上首頁，>10 分轉紅 |
| A9 | 高 | 環境衛生 `anomaly`/`confirmed_at` 有顯示但永不寫入（HACCP 缺異常判定與複核簽核） | `freezer-fridge.js:30,67` vs `218-247` | save 依規則算 anomaly；加 confirm 端點 |
| A10 | 高 | 溫度不與合規值比對、無超標告警、無列印匯出（compliant_temp 是自由字串） | `freezer-fridge.js:226-238` | 改 min/max 數值欄＋超標告警＋月報列印（照 training printShell） |
| A11 | 中 | 刪除冷凍庫房→歷史紀錄變孤兒（entries_json 參照 warehouseId） | `freezer-fridge.js:176-179` | 改軟刪或有歷史即擋 |
| A12 | 高 | 群發/公告無防重發、無逐群組結果落地、無補發、無排程；公告重送覆寫發送紀錄 | `broadcast.js:902-986`、`announcements.js:404-450` | 加 broadcast_recipient_log＋「只補發失敗」＋重送需確認 |
| A13 | 高 | 每日摘要 Flex 只列 30 項不提示截斷，卻寫「逾時視為確認」 | `lib/daily-summary-push.js:44`、`93` | 補「另有 N 項」＋LIFF 連結或分則推送 |
| A14 | 中 | 每日摘要推播結果無處可查、無補發入口（claim 是 at-most-once，失敗即永久漏） | `dist/index.js:427-431` | 結果寫 data_change_log＋設定頁顯示＋補發鈕 |
| A15 | 高 | 北農/畜產行情快照無排程，只在有人開頁才產生（連假永久缺快照；UI 文案卻寫「每日自動抓存」） | `lib/wholesale-snapshot.js:57-85`、`logistics.js:360`；`dist/index.js` 無行情排程 | 加每日排程（claimDailyMark 冪等）抓當日＋回補 3 天＋失敗告警 |
| A16 | 高 | 報價 finalized 不鎖定：價格可事後任意改且無軌跡 | `quotes.js:766-810`、`852-861` | finalized 後 save 拒絕（先解除＋稽核）；價格變更寫軌跡 |
| A17 | 高 | 報價品項增刪/整份刪除無稽核無軟刪，錯誤靜默吞（守則#4）；quote 表不在備份 | `quotes.js:833-880` | 三支補 logDataChange（刪前快照）＋錯誤回顯 |
| A18 | 中 | 報價「發送給客戶」只寫一個 app_settings 旗標（不記給誰/何時/哪版） | `quotes.js:733-742`、`lib/quote-report.js:842` | 改 quote_send_log，儀表板顯示已發 x/y 家 |
| A19 | 中 | 路線戰情室完整實作但零路由（死碼；calendar.js:119 文案還指向它） | `lib/route-war-room.js`、`admin/index.js:108` | 補頁或移除 |
| A20 | 中 | 行事曆無稽核、無編輯、驗證失敗靜默 redirect | `calendar.js:14`、`175-191` | 補稽核＋err 訊息＋編輯路由 |
| A21 | 中 | 教育訓練課程/簽到/問卷/成效 save 全無稽核（簽到是 DELETE+重插）；問卷不綁員工 | `training.js:608-643`、`813-833`、`834-841`、`850-866` | 四支補 logDataChange；問卷可選員工 |
| A22 | 中 | 缺「每人年度受訓時數/未達標名單」（TTQS 要看的） | `training.js:115-183` | 員工名冊加年度時數欄＋門檻紅標 |
| A23 | 中 | 舊每日盤點 `confirmed_at` 同樣永不寫入 | `inventory.js:2537` vs `2543-2573` | 補確認端點或移除 UI |
| A24 | 中 | 安全庫存有資料無告警/補貨建議；只掛舊自建倉不掛凌越料號 | `inventory.js:2707-2719`、`2802` | safety 改掛 erp_stock_items＋每日低量推播 |
| A25 | 中 | 稽核頁無分頁/日期範圍/匯出（上限 500 筆，追溯幾天就斷） | `analysis.js:20`、`50-53` | 加 date range＋cursor 分頁＋CSV |

### B. 專業設計項目

| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| B1 | 高 | 備份表清單 22 張漏掉盤點/調整/報價/訓練/公告/空籃/現金/group_features/條碼/稽核全域，文案卻稱「內容含盤點」 | `export-backup.js:74-81`、`104` | 改 information_schema 全表白名單反轉＋修文案（併 §五 B8） |
| B2 | 高 | 完全沒有還原流程（JSON 拿到手也匯不回去） | `export-backup.js:97-136` | 加 restore（dry-run＋交易＋稽核）或至少寫還原 SOP |
| B3 | 中 | 備份整表載記憶體再 stringify（長大後 Cloud Run OOM） | `export-backup.js:124-135` | 改串流輸出/NDJSON＋分批 |
| B4 | 高 | 效期品設定先刪後插無交易、批次無交易、零稽核（條碼對照同情境已包好，這是漏網雙胞胎） | `inventory.js:3281-3282`、`3296-3303`（對照組 `3140-3182`） | 三動作包交易＋logDataChange |
| B5 | 中 | 倉庫設定整張 DELETE 再插（部分送出即滅失中文名/include_stocktake）、無稽核 | `inventory.js:3039-3054` | 改逐倉 upsert＋稽核 include 變更 |
| B6 | 中 | stock/refresh 三筆 app_settings 分開寫無交易（半套→代理重推錯公司）；無節流 | `inventory.js:2894-2896` | 包交易或合併單鍵＋30 秒節流 |
| B7 | 中 | 行情快照無唯一鍵無鎖：併發 refresh 寫出重複列，加權均價失真 | `lib/wholesale-snapshot.js:18-27` | advisory lock＋(date,market,crop) 唯一索引改 upsert |
| B8 | 中 | check-then-insert 無交易/upsert 三處（併發撞 PK 直接 500，整天資料重打） | `freezer-fridge.js:240-246`、`inventory.js:2565-2571`、`training.js:850-860` | 改 ON CONFLICT / INSERT OR REPLACE 雙分支 |
| B9 | 高 | 90 天快照到期後歷史「對最新盤差」永久失去定案值（07-26 凍結修正只解 90 天內） | `lingyue-writeback.js:363-366`、`inventory.js:83` | 盤點送出/換日時把 latest_sys/diff_latest 定案寫進 stocktake_count |
| B10 | 中 | 行情/畜產/data_change_log/gemini_usage_log 無保留策略（行情表還在備份包裡逐月變胖） | `wholesale-snapshot.js:12-28`、`livestock-price.js:180-193`、`db/index.js:608` | 各給可調保留天數＋排程 prune（併 §五 B7） |
| B11 | 中 | loadWholesaleCropHistory 全表掃該作物再 JS slice；crop_name 無索引 | `wholesale-snapshot.js:145-146`、`170` | SQL 加日期下界＋(crop_name,record_date) 索引 |
| B12 | 中 | 目前庫存整份品項 JSON 內嵌頁面（上萬料號＝數 MB 頁面；photo 全表掃） | `inventory.js:2697`、`2752`、`2818` | 改分頁 API 首屏 200 筆 |
| B13 | 中 | loadStocktakeDay 每倉 N+1 查詢且為顯示一倉載入全部倉 | `inventory.js:106-140`、`156` | 頁面只載 selWh＋GROUP BY 彙總；CSV 才全量 |
| B14 | 中 | 盤差報表逐日×逐倉 3 查詢無區間上限（一年×10 倉≈1.1 萬次查詢直接超時） | `inventory.js:3506-3531`、`3556` | 限 ≤92 天＋range 查詢一次撈 |
| B15 | 中 | 舊盤點非數字原樣存 JSON→盤差報表整列靜默消失（與 stocktake-api 已強化驗證落差） | `inventory.js:2555-2559`、`3518-3524` | save 端比照 submitStocktake 逐格驗證回顯 |
| B16 | 低 | training CSV 每課 2 查詢 N+1 | `training.js:662-672` | GROUP BY 先撈 Map |
| B17 | 低 | 排查表記住群組寫入整段吞錯 | `inventory.js:1031` | 至少 console.warn |
| B18 | 中 | 必盤基準用公司總量 vs 盤點 sys 分倉優先不同源（他倉進出→假必盤；總量抵銷→漏必盤） | `lib/stock-mustcount.js:43-51` vs `stocktake-api.js:71-77` | computeMustCount 改分倉優先同一套 |
| B19 | 低 | computeMustCount 整段空 catch（必盤失效完全靜默） | `stock-mustcount.js:68`、`stocktake-api.js:115` | console.warn＋前端標「必盤暫不可用」 |
| B20 | 中 | 硬編碼閾值散在多處應進 app_settings（盤差 5% 兩處、逾期 1.5 倍兩處、AI 單價表） | `inventory.js:274/1282`、`dashboard.js:109/594-610`、`analytics.js:246`、`route-war-room.js:17-18` | 抽 settings 各處讀同一把 |
| B21 | 中 | 匯入品項逐列 INSERT 無交易無預覽無稽核（對照 import-erp 正確樣板） | `imports.js:73-92`（樣板 `inventory.js:2640-2657`） | 包交易＋dry-run 預覽＋稽核 |
| B22 | 中 | 用 UTC 取「今天」：台灣 08:00 前預設日期差一天（環境衛生/舊盤點/行情頁） | `freezer-fridge.js:27,181,222`、`inventory.js:2498`、`logistics.js:257,407,424` | 換 getTaipeiCalendarDateYYYYMMDD（freezer-fridge 解構了沒用） |
| B23 | 低 | 品項照片端點無公司隔離（跨公司撞號互看互蓋）、上傳/刪除無稽核 | `inventory.js:2827-2884` | 補稽核；長期 PK 改 (icpno,erp_code) |
| B24 | 中 | 刪除舊庫房連帶硬刪 daily_inventory 歷史、無稽核快照 | `inventory.js:2354-2368` | 改軟刪或刪前快照進 meta |
| B25 | 低 | dashboard_events 增刪無稽核；刪不存在也回 ok:true | `dashboard.js:567-590` | 加存在檢查＋稽核 |

> 共通病灶：「欄位有、顯示有、就是沒有寫入路徑」——`freezer_fridge_daily.anomaly/confirmed_at`、
> `daily_inventory.confirmed_at`、`stocktake_session.status` 三處一次盤清（實作簽核或移除 UI）。
> 「有交易＋有稽核」正確樣板已在 repo（條碼 3177、庫存調整 631、ERP 匯入 2640、盤點送出 stocktake-api 183），
> B4/B5/B6/B8 都是同情境漏網雙胞胎，照樣板補即可。
> 零 smoke test 的域：export-backup、freezer-fridge、training、broadcast/announcements、calendar。

---

## 四、設計規範遵循度＋LIFF UX（總評約 68/100）

核心結論：**規範不是被忽視，而是被「複製而非引用」**——每次新頁照上一頁抄一份改前綴，
視覺看似一致，改欄寬/加深色模式時才發現要改四個地方。

### 4.1 圖示（§3.1）
- 【高】LIFF 頁標題全用彩色 emoji：`basket-log.html:67,73,78,98`（📦🔢⬜⭕）、
  `customer-lookup.html:46,179-182`（👤📝🛒📨📅）——正是規範點名禁用清單。改 inline SVG（stocktake.html 已有先例）。
- 【中】`freezer-temp.html:52` 🌡️（`SF_ICONS.thermo` 已定義卻零使用）、`scan.html:652` 📷、
  `stocktake.html:539` 📦（同位置 scan.html:444 用 ▦——姊妹頁圖示不一致）。
- 【中】**10 個已定義 SF_ICONS 鍵零使用**（menu/chev_d/chev_r/refresh/scale/thermo/truck/dots/money/pin），
  同時各頁用文字符號土炮同一件事：`index.js:2080` ☰、`inventory.js:465` ↻、`268` ✎、
  `order-detail.js:556` ✎/`651` ▾/`751` ↻、`products.js:1086-1143` ▦✎⚖▤、`orders.js:171/264` ▦、
  `logistics.js:686` 🔺🔻、9 處 ✕ ＋ 7 處 ×（同一動作兩種符號）。一次性掃檔替換。
- 【低】`_shared.js` 實際 50 鍵 vs 設計指南 §3.1 只列 44（缺 building/bulb/info/phone/robot/trendingUp）；文件同步。

### 4.2 滑桿（§3.3）
- ✅ 規範點名的 `stk-seg`/`pe-switch`/`qe-seg` 已全數清零；`.sf-switch`×19、`.sf-seg`×21 已是主力。
- 【高】`inventory.js:1339` `sf-seg-btn`——**全 repo 唯一且無任何 CSS 定義、無 .sf-seg 父容器**：
  庫存統計頁公司切換器渲染成四個裸連結、看不出選中。改標準 `.sf-seg` 寫法（同檔 733/2999 正確）。
- 【中】`sf-btn-sm` 未定義 class（正確是 `.sf-btn.sm`）：`cash.js` 11 處＋`baskets.js` 5 處共 16 顆按鈕尺寸失效。
- 【中】`ivs-seg`（`inventory.js:1436-1548`）掛了但無樣式的死 class，刪除即可。
- 【中】LIFF 自刻 `.seg`/`.co-seg`/`.tog`（`stocktake.html:35-71`、`scan.html:29-31`、`scan-tool.html:29-31`）：
  `.tog` 是二元開關該用 `.sf-switch`。建議把 admin 的滑桿 CSS 抽片段由 `serveLiffPage` 注入（機制同 `__LIFF_ID__`）。

### 4.3 版面（§3.2）
- 【高】`inventory.js` 全部 17 頁、`cash.js` 全部 4 頁**零 sf-root**——全系統最資料密集的頁被 1600px 夾住，
  熱力圖在橫向捲、螢幕空一半。每頁最外層包 `<div class="sf-root">` 即生效。
- 【中】`analytics.js:116` 套了 sf-root 又自寫 `max-width:1200px` 自我抵銷；刪掉。
- 【中】training/freezer-fridge/quotes 三個資料密集頁也該套。
- 【低】`body.sf-fullwidth` 定義了全 repo 零使用（死規範）：用起來或從規範刪掉。

### 4.4 表格
- 【中】inline `<td/th style>` 共 372 處（cash 65、analysis 45、training 33、logistics 31…）。
  最該修：`analysis.js:585-593/688-692`、`order-detail.js:1906`、`index.js:2396-2414`——重寫的正是預設 table 已提供的東西。
- 【低】`quotes.js:186/216/225` 用 grid 假裝表格（RWD 斷點手動維護、無行列語意）。

### 4.5 三欄版型 sf3（§3.4）——**文件與程式不符**
- 實測 `sf3-grid` 只有 `orders.js:438` 一處。CLAUDE.md 宣稱「訂單審核/盤點/庫存統計已套」不成立：
  - 【高】每日盤點自刻 `stk-grid`+`wh-panel`（`inventory.js:358-365`）＝sf3 逐條抄、只差 18px 欄寬。
  - 【高】庫存統計自刻 `ivs-grid3/col/row/tag`（`inventory.js:1352-1359`）＝**sf3 100% 逐像素複製只換前綴**。
    兩處改回 `sf3-*` 是「刪最多碼、零視覺風險」的一筆。
- 【中】目前庫存 `stk-rail`（`index.js:2158-2170`）＝三欄變體第三種寫法（同一條 3px 藍條）。
- 【中】規範點名該套未套：松富銷貨統計（`cash.js:287-291` cash-2pane）、現金收款（`548-549`）、
  空籃記帳、稽核軌跡、冷凍檢查表（月曆→跳頁可併三欄免跳頁）。
- 【中】**四套月曆並存**（sf3-cal / cash-cal / calendar 的 cal-cell / freezer-cal），換月互動四種。抽共用 helper。

### 4.6 LIFF UX
- 【高】i18n 覆蓋斷裂：`basket-log`、`freezer-temp` **零 i18n**（使用者與盤點頁同一批倉庫/司機；
  freezer-temp 是食安判定頁）；`employee-bind` 六條 setStatus 全中文。
- 【高】`scan.html` 宣稱雙語但 11 處硬編碼中文：暫停/繼續鈕（797/815）、每列「帳上」（849）、
  倉庫為空的三步指引（440）、載入中（145）、網路不穩（411-412）、LIFF 錯誤（415/421）等。
  補 10 個 I18N 鍵（stocktake.html 已有可照抄）。
- 【中】語言鈕只在倉庫選擇畫面（scan 進掃描後無法切換），且切換會重打 API。
- 【高】**零離線偵測**（全 repo 無 online/offline 監聽；scan.html:410 註解自認倉庫網路不穩）；
  送出失敗訊息未告知「草稿已保住」（stocktake:811/scan:902）——守則#4 違規，而 stocktake:342 的續登路徑已示範正解。
- 【中】草稿清理只有 stocktake 有 `cleanOldDrafts()`；scan/basket-log/freezer-temp 三頁 localStorage 無限累積。
- 【中】觸控目標：盤點/掃碼（戴手套主戰場）反而最小——`.seg`/`.co-seg`/`.tog`/`.del` 全 26-33px；
  freezer-temp（44px）與 basket-log（44/48px）已合規可當範本。統一 min-height:44px 或 `::before` 熱區。
- 【中】`scan.html:5` `user-scalable=no`（WCAG 1.4.4 違規、老花無法放大）；改 `touch-action:manipulation`。
- 【中】LIFF 設計 token 兩家：scan/stocktake 用 `#2383e2`（正解），basket-log/customer-lookup/freezer-temp/order-review
  用 `#1d4ed8` 且變數命名不同（--txt2 vs --txt-2）。統一 A 家族。
- 【中】API 錯誤回英文 dev 字串直接顯示給使用者（`liff/index.js:226,246,384,440,472,721,758,800,804,884`）；改繁中＋修正指引。
- 【低】toast 七種實作；stocktake 全用阻斷式 alert（打斷連續盤點）。抽共用 toast。

### 4.7 一致性與無障礙
- 【中】公司切換器 5 種做法、日期選擇器 4 種、匯出按鈕 3 種 class＋5 種文案（正解範本 `customers.js:866`）。
- 【中】**頁首兩代並存**：規範寫 notion-*（L154），實際新頁全走 sf-*，另有 18 處硬刻 `<h1 style>`。
  必須擇一寫進規範（建議 sf-* 為正統＋加 `.sf-page-title`），否則每個新對話都在賭。
- 【高】**全站零 focus 樣式**（.sf-btn/.sf3-row/.sf-seg/.sf-tab 皆無 :focus-visible）：一行 CSS 修好全站。
- 【中】LIFF 搜尋框 `outline:none` 無替代（stocktake:66、scan:121；WEB 模式是桌面鍵盤場景）。
- 【中】9 組 modal/sheet 缺 Escape；`role="dialog"` 全 repo 僅 2 處；無焦點歸還。抽 `sfModal` helper。
- 【低-中】約 10 處輸入框只有 placeholder 無 aria-label（盤點數量欄建議 `aria-label="{品名} 盤點數量"`）。
- 【低】盤點 WEB 模式 `.qty` 無 Enter 跳下一列（最核心重複操作）；`.more`/`.exp-chip` 加 tabindex=-1 讓 Tab 只走數量欄。
- 【低】`calendar.js:56` `href="javascript:void(0)"` 改 `<button>`。

### 建議執行順序（前三批合計約半天）
1. **批次 A（零風險刪碼）**：ivs-* / stk-grid → sf3-*；修 sf-seg-btn、sf-btn-sm；analytics 刪 max-width。附 smoke test。
2. **批次 B（一行級）**：全域 :focus-visible；inventory/cash 包 sf-root。
3. **批次 C（LIFF 共用層）**：滑桿 CSS/:root token/toast/cleanOldDrafts 抽片段由 serveLiffPage 注入。
4. **批次 D（文件同步）**：更新 CLAUDE.md L164（sf3 實況）、L154（定 sf-* 或 notion-* 正統）、§3.1 補 6 鍵。
   **這批不做，前三批成果會在下一個對話被重新違反。**

---

## 五、資料庫／安全／基礎設施

### A. 認證／授權
| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| A1 | 高 | **LINE webhook 簽章 fail-OPEN**：未設 secret 時掛 `next()` 完全不驗，任何人 POST /webhook 可偽造建單/改單/觸發盤點 | `webhook/line.js:33`、`1886-1891`（對照 worker `index.js:112-119` 已 503） | 改一律 503＋smoke test |
| A2 | 高 | 整庫備份 JSON（含 admin_users 全部 pbkdf2 雜湊＋bind token）任何登入者可下載 | `export-backup.js:116-136`（無 requireManager） | /backup* 掛權限＋排除敏感 key＋下載寫稽核 |
| A3 | 高 | 權限模型三段（經理/canCash/移工不可刪）之外大量高破壞力路由零閘門：inventory/imports/orders/order-detail/ai-settings 等 17 檔 requireManager 零次 | 定義 `admin/index.js:3220-3223`、`3341-3347`、`2034-2041` | 升級能力表 {canVoid,canImport,canPrompt,canAdjustStock,canBackup}＋router.stack 掃描測試 |
| A4 | 中 | 「移工不可刪」是路徑字串比對，漏掉 void/unapprove/undo/count-edit 等等效破壞動作 | `admin/index.js:2034-2041` | 改明確破壞性路由白名單常數＋測試全掃 |
| A5 | 中 | 登入節流可被 X-Forwarded-For 偽造；`loginFails.clear()` 可被主動觸發清空 | `admin/index.js:3115-3118`、`3135` | trust proxy＋req.ip；key 改 username 為主；clear 改 LRU；達門檻告警 |
| A6 | 中 | Session token 無版本/無撤銷：改密碼後舊 cookie 仍有效 7 天；Secure 用 DATABASE_URL 推斷 | `admin/index.js:1989-1993`、`3144-3147`、`users.js:442-471` | token 加 iat vs pwChangedAt；Secure 改讀 proto/env |
| A7 | 高 | LIFF 盤點/掃碼 API 只驗「是 LINE 用戶」零授權：外部人可查全庫存、寫正式盤點、改條碼對照（空籃 API 已有正確授權可抄） | `liff/index.js:86-92`＋使用點；正解 `707-713` | stkAuth 後加員工綁定 or 群組成員＋group_features 檢查；至少 submit/bind 先修 |
| A8 | 中 | 員工綁定碼 Math.random 產生＋consume 無節流（可暴力猜 6 位數取得經理身分） | `lib/employee-line-binding.js:15-17`、`webhook/line.js:664-678` | crypto.randomInt＋5 次/10 分節流＋綁定成功通知 |
| A9 | 低 | LIFF bind token 有 Math.random fallback、過期永不 GC、consume 非原子 | `lib/liff-bind-token.js:19-25`、`71-76` | randomBytes＋發放時掃過期＋DELETE RETURNING |
| A10 | 中 | X-Writeback-Key 覆蓋完整（fail-closed＋timingSafeEqual ✅）但單一長期共用金鑰、無旋轉、cash 與庫存同一把 | `admin/index.js:3159-3180` | 支援兩把並存旋轉；cash-ingest 拆獨立金鑰；記來源 IP 顯示 |
| A11 | 中 | 全站無 CSRF token（SameSite=Lax 有擋主面）、無 CSP/HSTS/X-Frame-Options、無速率限制；GET 型刪除確認頁易誤解 | 全 repo helmet/csp 零命中 | 極簡 CSP＋XFO＋HSTS 全域中介層；login/liff api 加 token bucket |
| A12 | 低 | jobs secret 三把 fallback 一把通吃；`ALLOW_UNAUTH_JOBS=1` 明文逃生開關可全裸 | `dist/index.js:225-241` | 刪掉逃生開關或每次呼叫告警；推播類獨立金鑰 |

### B. 資料庫設計
| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| B1 | 高 | `admin_users` 是 app_settings 單一 JSON blob：每請求一次 SELECT+parse、load-mutate-save 全覆蓋 lost update（經理改職稱 vs LIFF 綁定並發互洗）、無唯一約束 | `admin/index.js:3041-3057`、`lib/employee-line-binding.js:71-110` | 新建 admin_user 真表＋冪等遷移＋單欄 UPDATE。**本輪最值得的結構性改善** |
| B2 | 高 | PG 遷移無版本表：236 條 DDL 每次冷啟動重跑、78 個空 catch、批次 try 第一條失敗後面全靜默跳過（服務照常啟動缺表） | `db/index.js:920-1705`（例 `1677-1695` 一個 try 包 10 條） | schema_migrations 版本表＋一語句一 try 不吞錯＋/health 回報 migrationError |
| B3 | 中 | /admin/orders 缺 `orders(order_date)` 索引（複合索引前導欄不對＝全表掃）＋每列 4 個子查詢（最壞 1500 次） | 索引 `db/index.js:282/1108`；查詢 `orders.js:38-53` | 加索引＋COUNT 改 LEFT JOIN 聚合 |
| B4 | 中 | 缺索引/唯一約束熱點：order_items(product_id)、data_change_log(actor_username)、logistics_orders(order_date)、stocktake_count(erp_code)、product_barcode 無 (icpno,barcode) 唯一索引兜底 | 各處 | 補齊（照 ux_customers_line_group「有重複只警告不建」範式） |
| B5 | 中 | SQLite 不啟用 `PRAGMA foreign_keys`，PG 有 29 個 FK → 本機插得進孤兒、雲端 23503（sqlForPg 擋不到約束層） | `db/index.js:148-159` | 兩處加 pragma＋pg-smoke 加孤兒斷言 |
| B6 | 中 | 時間兩套寫法並存：`created_at::date = ?`（UTC 直轉，台北 00-08 點紀錄算昨天，/audit 今日統計少 8 小時）vs 正解 `AT TIME ZONE 'Asia/Taipei'` | 踩點 `analysis.js:26/188-190`；正解 `dashboard.js:145/661/664` | 抽 `lib/sql-time.js` helper＋規則寫進 CLAUDE.md＋邊界測試 |
| B7 | 中 | 保留策略只覆蓋 erp_stock_*_daily 且掛在推送路徑：processed_line_messages/data_change_log/gemini_usage_log/line_collect_sessions/wholesale 快照全部無限成長 | 唯一清理 `lingyue-writeback.js:363-366` | 新增 `/api/jobs/retention-daily` 集中處理＋刪除列數留痕 |
| B8 | 高 | 備份/還原：人工按鈕 JSON、22/60+ 張表、無自動排程、無異地、**零還原程式碼**；docs 無 SOP | `export-backup.js:74-136`、`docs/資料長期保留-必讀.md` | 動態表清單＋restore 腳本＋Cloud SQL/Supabase 自動備份＋演練紀錄（併 §三 B1/B2） |
| B9 | 中 | PG 默認 `rejectUnauthorized:false`（公網 Supabase 可 MITM）；pool 無 statement_timeout/idle timeout（慢查詢吃光 8 條連線） | `db/index.js:909-916`、`892-896` | CA 進 Secret＋fail-closed 化；pool 加三個 timeout |
| B10 | 中 | 兩個關鍵檔含裸 0x00/0x01 位元組，grep/rg 視為 binary 靜默跳過（安全掃描/重構搜尋都會漏掉機器端點與庫存核心） | `lingyue-writeback.js` 行 294/384、`inventory.js` 行 2637/2643 | 改 `"\u0000"` 字面量＋lint 加控制字元檢查 |

### C. 可觀測性
| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| C1 | 高 | `ops_alert_group_id` 無設定介面/文件/seed：notifyOps 讀不到即永久靜默 no-op → 現有 7 個告警點（回寫三振/單號衝突/庫存推送失敗/刷新失敗/重取單失敗/LINE 漏單）全是啞的 | `lib/ops-notify.js:43-56` | LINE 設定頁加「系統告警群組」下拉＋測試鈕（照 stocktake_anomaly_group_id 做法）＋/health 回報＋儀表板紅字。**投入最小回收最大** |
| C2 | 高 | 告警覆蓋太窄：initDb 失敗、rhythm/daily-summary 排程失敗、Cloud Scheduler 沒打進來、uncaughtException 全部只 console | `dist/index.js:10-15`、`78-81`、`405-407`、`429-431` | process handler 加 notifyOps；新增 watchdog-hourly（心跳/快照/盤點數/進帳/Gemini 錯誤率） |
| C3 | 中 | 477 個 console.* 純文字：無 request id 串不起一則訊息的跨服務軌跡、無錯誤聚合、無告警政策進版控 | 全 repo | 極簡 `lib/log.js` JSON 輸出，LINE message.id 當 correlation id 先換收單鏈 |
| C4 | 中 | /health 是開機快照：DB 中途掛掉照回 200（Uptime/cloudbuild 閘門全綠）；無版本資訊 | `dist/index.js:39-60` | 加 SELECT 1 真探測（2s timeout＋10s 快取）＋回 revision/BUILD_SHA/migrationError |
| C5 | 中 | Gemini 成本只有畫面無門檻告警；價格表硬寫；失敗呼叫不記 usage log | `dashboard.js:658-705` | 月預算 app_settings＋80%/100% 告警；價格移 settings；記 429/500 |

### D. 測試覆蓋（現況 23 檔）
- 【高】**權限與認證零測試**：無任何「未登入導向 login／移工不能刪／非經理不能開 users／backup 需權限／無 secret webhook 拒收」斷言——A1-A4 修好也沒東西防退化，而拆檔還在搬路由。建議 `test/admin-authz.test.js` 路由×職稱矩陣＋「POST 路由必須登記」守衛。
- 【高】**cash 收款域 19 支路由 0 測試**（全域唯一涉及金額的模組）。至少三條：canCash 403／同 sp_no 沖兩次唯一約束／cash-ingest 冪等。
- 【中】broadcast（一鍵推全客戶、不可撤回）0 測試；training 29 支、logistics 行情解析、imports xlsx、LIFF 三個安全模組（liff-auth/verify/bind-token）皆 0。
- 【中】缺時區邊界與 retention 迴歸測試（人工測不到的兩類）。

### E. 依賴／供應鏈
- 【高】`xlsx@0.18.5`：原型污染＋ReDoS，npm 無修版（修版只在 cdn.sheetjs.com）；上傳中介層無 fileFilter，且 imports 無 requireManager。改裝 cdn tarball 0.20.3 或換 exceljs＋加 fileFilter。
- 【高】**Node 20 已過 LTS 維護期**（2026-04-30 EOL）且 base image 用浮動 tag `node:20-slim`（Dockerfile:2、cloudbuild:16/42）。升 node:22-slim＋pin 版本（先確認 better-sqlite3 需升 13.x）。
- 【中】npm audit 11 個漏洞、主要依賴全面落後（@line/bot-sdk 落後 2 個 major——簽章驗證就在這包）；無 .github/（無 Dependabot/Actions）；cloudbuild 用 `--no-audit` 明確關掉。加 dependabot.yml＋audit 閘門（白名單 xlsx）＋排 bot-sdk/tasks 升版 PR。

### F. 部署
| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| F1 | 高 | 部署後健檢失敗但流量已 100% 導入（註解自認需手動回滾） | `cloudbuild.yaml:98-123` | 金絲雀兩段式：--no-traffic → 打 candidate /health → 通過才導流 |
| F2 | 高 | `max-instances=1` 不變式只在 Console：誤改即靜默打破 collect session/節流/去重的單實例假設且無自我檢查 | `cloudbuild.yaml:98-110`（無資源參數） | deploy 明確帶 --max-instances=1 等參數＋實例指紋偵測告警；中長期 session 全落 DB |
| F3 | 中 | Secret Manager 有完整文件但實際 cloudbuild 路徑沒用：所有金鑰是 Cloud Run 明文 env | `docs/secret-manager-and-line-rotate.md` vs `cloudbuild.yaml:3-9` | deploy 加 --update-secrets；文件補「實際走 cloudbuild」警示 |
| F4 | 中 | cloudbuild 無 timeout 欄位（預設 10 分鐘易被砍）；兩次 npm ci 重複；缺 node --check/route-map/audit 閘門；/admin 全公網無 Cloud Armor | `cloudbuild.yaml` | timeout 1800s＋合併步驟＋補閘門＋評估 IP 限制 |
| F5 | 中 | env 分散五處無權威清單、Cloud Scheduler 定義只在 docs 敘述（重建＝考古） | `.env.example` 等 | 啟動 env 檢查＋/health 回 missingEnv＋docs 權威表＋setup-schedulers.sh |

---

## 六、凌越串接鏈路＋文件一致性

### 6.1 串接可靠性
| # | 嚴重度 | 項目 | 位置 | 現況／建議 |
|---|---|---|---|---|
| 1.1 | 高 | ops 告警群組無設定入口（同 §五 C1，兩路獨立命中） | `lib/ops-notify.js:43-52` | 見執行摘要 #2 |
| 1.2 | 中 | 回寫「結果不明」已修（07-14 §2.6）但回查失敗（0 命中/多筆/回查本身炸）一律回 None＝重派再寫一張，且無告警 | `ly_agent_gui.py:628-648`、`ly_writeback_bridge.py:144-187` | 分流 `{ok:false,unknown:true}` → 移出自動重試＋標錯誤＋notifyOps 由人決定 |
| 1.3 | 中 | /pending 盲掃還在且有 `?scope=all` 逃生門（07-14 §2.7 修一半）；GUI 測試連線綁著它 | `lingyue-writeback.js:20-75`、`ly_agent_gui.py:374-394` | 加 /ping 探針端點；scope=all 只回單號清單或移除 |
| 1.4 | 高 | 多公司推送部分失敗被後續成功洗掉：00 失敗→01 成功清 error＋status=done→前端顯示「已更新」（**松富失敗報成功**） | `ly_stock_push.py:453-465`、`lingyue-writeback.js:431-433`、`595-601` | 狀態鍵全改 per-company；push_once 回 (total,errors) 逐家回報 |
| 1.5 | 中 | 推送失敗/快照過期無常駐標示（錯誤只在按按鈕後輪詢可見；代理活著凌越壞掉心跳仍綠） | `inventory.js:2671-2674`、`2806` | snapLabel 依 age 上色＋常駐 banner 讀 refresh_error |
| 1.6 | 中 | inventory-wait/cash-refresh-wait 是至多一次投遞（先清旗標，回應丟失＝請求永久消失）；請求槽單格（兩人點不同公司互蓋） | `lingyue-writeback.js:571-575`、`637-638` | 改租約制（claimed→report 結案）＋公司請求改累加集合 |
| 1.7 | 中 | 代理離線/凌越鎖定無主動告警（心跳有寫但雲端無定時檢查）；固定 5 秒重試無退避 | `ly_agent_gui.py:773-799`、`ly_stock_push.py:374-379` | 排程 15 分鐘心跳看門狗＋指數退避 |
| 1.8 | 中 | 訂單「排隊中/寫入中」後台不可見（與從沒轉入長一樣；代理離線沒人知道 N 張卡佇列） | `orders.js:319` | 加 ⏳排隊中 pill＋queued>15 分告警 |
| 1.9 | 低中 | 四條長連線各 1.5 秒輪詢＋每連線寫心跳 upsert（全天約 1.4 萬次 app_settings 寫入） | `lingyue-writeback.js:83-88/144` | 輪詢 3s＋心跳 >60s 才寫 |

### 6.2 代理 GUI 功能缺口
- 【中】無檔案日誌（記憶體 800 行、關窗失憶、`_log_once` 吞重複錯誤）→ 滾動 `ly_agent.log` 留 7 天＋最近錯誤面板。
- 【中】**版本可見性為零**：熱替換部署模型下 GUI 不顯示實際載入的 .py 版本/mtime、心跳不帶版本
  （2026-07-09 撞號事故根因「內網跑舊 exe」現在仍無法自證）→ `__version__`＋GUI 顯示＋`X-Agent-Version` 上報。
- 【低中】缺「手動重推單一公司」按鈕（引擎已支援 icpno_override）；journal 殘留無可視化/手動重送。
- 【低】推送排程只有固定時刻無間隔式；三個生效設定（wb_maker/purchase_idakd/autostart）無 UI；
  設定視窗金鑰標籤寫錯（寫 `LINGYUE_WRITEBACK_KEY`、實讀 `LY_WRITEBACK_KEY`——三份文件跟著錯的源頭）。
- 【中】多公司部分失敗 GUI 不顯著（只有全滅才標紅）；`cash-ingest` 無 icpno 兩位數守門
  （`all` 誤傳靜默落到 00 松富，inventory-push 則會明確拒絕——不一致）。

### 6.3 設計（殘留檔案／部署／冪等）
- 07-14 §6 清單狀態：四支過時腳本＋三個 bat **已刪除** ✅；仍未做：現役代理檔未收 `scripts/ly_agent/` 子夾、
  **`ly_order.py`/`ly_datain.py` 仍不在版控**（原子性/流水號邏輯無法審查，撞號防護只能 monkey-patch）。
- 重複邏輯：`first_icpno` 兩份、`all` 展開三份＋白名單一份（**新增公司要改 4 處**——07-11 漏松揚就是這結構）、
  timeout client 每支腳本一份。抽 `ly_common.py`。
- 【中】`deploy-cloud-run.mjs` 預設仍會洗環境變數（踩過事故只留口頭禁令）→ 反轉預設為 --keep-env。
- 【中】機器端點無稽核（lingyue-writeback.js 解構了 logDataChange 整檔零使用；重轉清舊 doc_no 無舊值紀錄）。
- 冪等盤點：inventory-push/cash-ingest/callback/wait ✅；inventory-wait/cash-refresh-wait ⚠（見 1.6）；
  snapshot_at 完全信任代理時鐘（偏差>30 分建議改伺服器時間）；txn-request 鍵無 TTL 永久累積。
- 【中】凌越鏈路 smoke test 只有金鑰 401/503：callback 三態/wait 租約/推送交易/queued-only 過濾全靠人工。補 `test/lingyue-writeback.test.js`。

### 6.4 文件一致性（過時清單）
| 文件 | 嚴重度 | 主要問題 |
|---|---|---|
| `凌越回寫-工作交接.md` | 高 | §5 欄位對映整段是作廢的銷貨單 SP_/SD_（現行是訂貨單 OR_/OD_）；:40 說 bridge 在 lystk_tool 與 CLAUDE.md 定案**直接相反**（照做就踩「舊版蓋新版」）；§6-7 是已完成的歷史待辦。CLAUDE.md:172 還把它列權威。→ 檔頭標過時、索引改指 `資料處理規則.md` |
| `凌越訂單回寫-串接說明.md` | 高 | 同檔自我矛盾三處（/pending 語意、--apply 必要性、ly_datain 依賴）；SP_/SD_ 表作廢；無 /wait/journal/callback 語意 |
| `凌越-目前庫存-庫存管理.md` | 中 | 架構圖仍以已刪除的 ly_stock_agent.py 為主角（同檔自我矛盾）；金鑰 env 名錯；「03=桂田」錯（是松成）缺 02；缺 erp_stock_daily/OHLC 落地行為 |
| `凌越整合代理-視窗程式.md` | 中 | 教已刪除的腳本當正常用法；按鈕清單與實際不符（「回寫測一張」不存在）；缺銷貨代理與 txn 線；設定表金鑰名錯、「預設倉別」兩列互相矛盾 |
| `凌越回寫-規則與必備設定.md` | 中 | 金鑰名錯；OR_CHECK 預設同檔矛盾（:48 未審核 vs :91 已審核 vs 程式 True）；倉別規則與實作矛盾；端點速查缺 9 支中的 6 支 |
| `內網設定與部署.md` | 低中 | 「03=桂田」錯缺 02；仍列 ly_datain 必備；程式一覽缺三支現役檔（ly_sales_push/ly_item_txn/ly_query_daily_sales）；env 表缺 LY_SALES_TIMES/LY_FUTURE_DAYS/LY_PURCHASE_IDAKD/LY_QUERY_PORT |
| `松揚-掃碼盤點.md` | 低 | 停在 07-10，未含 07-21 掃碼頁中越雙語/共用 stk_lang/草稿鍵含公司代碼，也未回填 07-14 §2.5 掃碼草稿覆蓋的處置結論 |
| `系統邏輯總檢視-2026-07-14.md` | 低 | §2.6/2.7/2.8/§6 已部分或全部處理但報告本身無「已修/未修」標記，新對話易當待辦重做 |
| `CLAUDE.md` 自身 | 中 | :49-50 說「兩條背景執行緒」實際四條（stock/writeback/txn/sales）；整節沒提每日帳款收款鏈路；:48 只寫雲端側 `LINGYUE_WRITEBACK_KEY` 未寫內網側 `LY_WRITEBACK_KEY`（三份文件錯的根源）；:172 把過時的工作交接列權威、真正權威 `資料處理規則.md` 沒進索引；:174 引用的「規則與必備設定.md §9」在 repo 內同名檔只有 §0-§8 |

---

## 七、跨路去重與重疊對照

同一問題被多路獨立發現（互為佐證、置信度最高，處理時一次解決）：

| 問題 | 出現路數 | 位置 |
|---|---|---|
| **ops_alert_group_id 告警全啞** | §五 C1、§六 1.1 | `lib/ops-notify.js:43-56` |
| **備份表不全＋無還原流程** | §三 B1/B2、§五 B8 | `admin/export-backup.js` |
| **webhook fail-open** | §一 B21、§五 A1 | `webhook/line.js:1886-1891` |
| **機器端點無稽核** | §二 B1、§六 6.3 | `admin/lingyue-writeback.js`、`order-detail.js` |
| **客訴/作廢單清凌越佇列** | §二 B5、§六（隱含） | `complaints.js:29-31`、`lingyue-writeback.js:110` |
| **UTC 取「今天」差一天** | §一 B15、§三 B22、§五 B6 | webhook/inventory/freezer/logistics/analysis 多檔 |
| **多外部呼叫無 timeout／凌越側無退避** | §一 B11、§六 1.7 | gemini-*、LINE 下載、凌越推送 |
| **Flex/摘要截斷不提示** | §一 B7、§三 A13 | `line-flex-messages.js`、`daily-summary-push.js` |
| **設計「複製而非引用」（sf3 三套 clone）** | §四（三處） | `admin/inventory.js` 多段 |
| **max-instances=1 記憶體狀態前提** | §一 B16、§五 F2 | `cloudbuild.yaml`、`webhook/line.js` |

---

## 八、方法備忘（下次照抄）

- **六路並行代理**（各給明確檔案範圍＋排除已知 backlog）＋主控核對最高嚴重度宣稱，是本輪高性價比做法。
  本輪最高三項資安宣稱已就地核對程式碼屬實（見開頭註記）。
- ⚠ `admin/lingyue-writeback.js` 與 `admin/inventory.js` 含裸 0x00/0x01 位元組，`grep`/`rg` 會靜默當 binary 跳過——
  安全掃描務必 `grep -a`／`python` 讀，或先修 §五 B10。（本報告一度也被同類位元組污染，已清除。）
- 「欄位有、顯示有、無寫入路徑」是本系統一種系統性病灶（簽核類），值得每輪專門掃一次。
- 「複製而非引用」（sf3 clone、first_icpno 兩份、all 展開四處）比「不遵守」更難察覺，
  需刻意找「幾乎一樣但不完全一樣」的重複。
- 建議節奏：每月或每次大改後重跑這六路掃描，加上 `npm run lint`＋`npm test`＋PG 冒煙三道 CI 閘門。